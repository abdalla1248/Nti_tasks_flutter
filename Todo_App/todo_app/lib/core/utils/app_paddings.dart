import 'package:flutter/material.dart';
import 'package:flutter_screenutil/flutter_screenutil.dart';

class AppPaddings {
  static EdgeInsets screen = EdgeInsets.all(20.w);
  static EdgeInsets card = EdgeInsets.all(16.w);
  static EdgeInsets formField = EdgeInsets.symmetric(horizontal: 16.w);
  static EdgeInsets button = EdgeInsets.symmetric(vertical: 12.h, horizontal: 24.w);
  static EdgeInsets listItem = EdgeInsets.symmetric(vertical: 8.h, horizontal: 16.w);
}
